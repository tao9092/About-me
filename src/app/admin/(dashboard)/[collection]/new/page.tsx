import{notFound}from"next/navigation";import{ContentForm}from"@/components/content-form";import type{EntityTable}from"@/lib/content";const map:Record<string,EntityTable>={competitions:"competitions","upcoming-competitions":"upcoming_competitions",certificates:"certificates",education:"education",projects:"projects",experiences:"experiences",awards:"awards",skills:"skills",links:"links"};export default async function Page({params}:{params:Promise<{collection:string}>}){const key=(await params).collection;const entity=map[key];if(!entity)notFound();return <><header className="admin-header"><div><p className="eyebrow">New draft</p><h1>Add {key.replaceAll("-"," ")}</h1><p>Fill the essentials now; enrich and publish when ready.</p></div></header><ContentForm entity={entity}/></>}
