import{logoutAction}from"@/app/actions/auth";import{requireAdmin}from"@/lib/auth";export default async function Page(){const user=await requireAdmin();return <><header className="admin-header"><div><p className="eyebrow">Security</p><h1>Account</h1><p>The single owner account configured by ADMIN_EMAIL.</p></div></header><section className="admin-panel"><h2>{user.email}</h2><p>Authentication is managed by Supabase. Change the password in Supabase Auth or use its recovery flow.</p><form action={logoutAction}><button className="button button-secondary">Sign out</button></form></section></>}
