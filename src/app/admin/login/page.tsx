import Link from "next/link";import{redirect}from"next/navigation";import{getAdmin}from"@/lib/auth";import{LoginForm}from"@/components/login-form";export default async function Page(){if(await getAdmin())redirect("/admin");return <main className="auth-page"><div className="auth-card"><p className="eyebrow">Owner access</p><h1>Welcome back.</h1><p>Only the configured administrator can sign in. There is no public registration.</p><LoginForm/><Link href="/">← Back to portfolio</Link></div></main>}
