import type{Metadata}from"next";import{PublicDetail}from"@/components/public-detail";import{getPublicContent}from"@/lib/content";export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const row=await getPublicContent("projects",slug);if(!row)return{title:"Project not found",robots:{index:false,follow:false}};return{title:String(row.seo_title??row.title_en),description:String(row.seo_description??row.summary_en??"")}}export default async function Page({params}:{params:Promise<{slug:string}>}){return <PublicDetail table="projects" slug={(await params).slug}/>}
