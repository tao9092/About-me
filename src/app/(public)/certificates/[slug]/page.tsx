import type{Metadata}from"next";import{PublicDetail}from"@/components/public-detail";import{getPublicContent}from"@/lib/content";export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const row=await getPublicContent("certificates",slug);if(!row)return{title:"Certificate not found",robots:{index:false,follow:false}};return{title:String(row.title_en),description:String(row.summary_en??"")}}export default async function Page({params}:{params:Promise<{slug:string}>}){return <PublicDetail table="certificates" slug={(await params).slug}/>}
