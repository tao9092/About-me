import type{MetadataRoute}from"next";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL??"http://localhost:3000";return["","projects","competitions","upcoming-competitions","certificates","education","experience","awards","skills","links","resume"].map(path=>({url:`${base}/${path}`,lastModified:new Date(),changeFrequency:"weekly",priority:path?0.7:1}))}
